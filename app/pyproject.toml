[project]
name = "support-portal"
version = "0.1.0"
description = "Fake e-commerce support portal for the DSWT Paris live demo"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic-settings>=2.5",
    "rapidfuzz>=3.10",
    "confluent-kafka[avro,json,schemaregistry]>=2.6",
    "mcp>=2.2.0",
    "httpx>=0.27",
]

[tool.uv]
package = false
